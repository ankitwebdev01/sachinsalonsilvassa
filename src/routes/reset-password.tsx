import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Reset Password | Sachin Salon"},{name:"description",content:"Set a new owner password."},{name:"robots",content:"noindex"},{property:"og:title",content:"Reset Password"},{property:"og:description",content:"Set a new owner password."},{property:"og:url",content:"/reset-password"}]}),component:ResetPage});
function ResetPage(){const nav=useNavigate();const [ready,setReady]=useState(false);const [password,setPassword]=useState("");const [message,setMessage]=useState("");useEffect(()=>{setReady(window.location.hash.includes("type=recovery")||window.location.search.includes("type=recovery"));},[]);async function submit(e:React.FormEvent){e.preventDefault();const {error}=await supabase.auth.updateUser({password});if(error)setMessage(error.message);else nav({to:"/dashboard"})}return <main className="grid min-h-screen place-items-center bg-secondary px-4"><form onSubmit={submit} className="w-full max-w-sm rounded-lg bg-card p-7"><h1 className="text-2xl font-extrabold">Set a new password</h1>{ready?<><Input className="mt-6" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><Button className="mt-4 w-full">Update password</Button></>:<p className="mt-4 text-sm text-muted-foreground">Open the recovery link from your email to continue.</p>}{message&&<p className="mt-4 text-sm text-destructive">{message}</p>}</form></main>}
